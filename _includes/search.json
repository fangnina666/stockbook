---
layout: null
permalink: /search.json
---

[
{% assign pages = site.pages | where_exp:'p','p.path contains "reports_his/"' %}
{% for p in pages %}
  {
    "title": {{ p.title | default: p.name | remove: '.md' | jsonify }},
    "url": {{ p.url | relative_url | jsonify }},:
    "content": {{ p.content | strip_html | replace: "\n"," " | jsonify }}
  }{% unless forloop.last %},{% endunless %}
{% endfor %}
]
